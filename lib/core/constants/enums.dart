enum TheStates {
  initial,
  success,
  failure,
}

// enum SearchResultType{
// 	task.entityService,

// }

enum RadioChangeValue {
  eSewa,
  khalti,
  masterCard,
  visaCard,
  googlePay,
  paypal,
  applePay
}
