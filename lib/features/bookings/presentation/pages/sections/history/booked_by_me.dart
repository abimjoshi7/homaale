import 'package:flutter/material.dart';

class BookedByMe extends StatelessWidget {
  const BookedByMe({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
